---
phase: 01-amputation-cleanup-tsc-green
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - berrygems/extensions/dragon-breath/index.ts
autonomous: true
requirements: [AMP-04]
must_haves:
  truths:
    - "tsc --project berrygems/tsconfig.json exits 0"
    - "dragon-breath/index.ts line 20 imports from ../../lib/settings.ts (two dots, matching dragon-guard/index.ts:3 and dragon-websearch/index.ts:3 precedent)"
  artifacts:
    - path: "berrygems/extensions/dragon-breath/index.ts"
      provides: "Corrected import path to ../../lib/settings.ts"
      contains: 'from "../../lib/settings.ts"'
  key_links:
    - from: "berrygems/extensions/dragon-breath/index.ts"
      to: "berrygems/lib/settings.ts"
      via: "relative import from directory extension (two-dot depth)"
      pattern: "from \"\\.\\./\\.\\./lib/settings\\.ts\""
---

<objective>
Fix the one-line import-path bug in `berrygems/extensions/dragon-breath/index.ts:20` that is currently the sole `tsc` error. This unblocks `tsc --project berrygems/tsconfig.json` as the verification gate for every downstream plan in this phase (AMP-01, AMP-02, AMP-03, AMP-05). Closes AMP-04.

Purpose: `tsc` RED is the blocker — no subsequent plan can rely on `tsc` as a gate until this is green. One-line fix, one-file change, wave-1 solo to isolate the unblock.

Output: `dragon-breath/index.ts` imports from the correct depth; `tsc` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md
@AGENTS.md
@berrygems/AGENTS.md

<interfaces>
Precedent pattern (verified in live tree, RESEARCH.md §AMP-04):

From berrygems/extensions/dragon-guard/index.ts:3:

```typescript
import { readPantrySetting } from "../../lib/settings.ts";
```

From berrygems/extensions/dragon-websearch/index.ts:3:

```typescript
import { readPantrySetting } from "../../lib/settings.ts";
```

Directory-extension import depth rule: `extensions/<dir-ext>/index.ts` reaches `lib/` via `../../lib/`, NOT `../lib/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix dragon-breath import path from ../lib/ to ../../lib/</name>
  <files>berrygems/extensions/dragon-breath/index.ts</files>
  <read_first>
    - berrygems/extensions/dragon-breath/index.ts (lines 1-40, confirm line 20 is the import to edit and no other `../lib/` paths exist)
    - berrygems/extensions/dragon-guard/index.ts (lines 1-10, confirm analog import depth `../../lib/settings.ts`)
    - berrygems/extensions/dragon-websearch/index.ts (lines 1-10, confirm analog import depth `../../lib/settings.ts`)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "AMP-04: Dragon-Breath Import-Path Bug"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md section "Shared Patterns > Import depth rule"
  </read_first>
  <action>
Per D-04 / AMP-04 (RESEARCH.md §AMP-04, verified one-line fix):

Edit `berrygems/extensions/dragon-breath/index.ts` line 20.

BEFORE (current):

```typescript
import { readPantrySetting, readPantryKey } from "../lib/settings.ts";
```

AFTER (target):

```typescript
import { readPantrySetting, readPantryKey } from "../../lib/settings.ts";
```

Do NOT modify any other line. Do NOT modify any other file in this plan. RESEARCH.md §AMP-04 "No other ../lib/ mistakes exist in dragon-breath" was verified by grep count = 1; confirm this assumption still holds by running the grep in acceptance criteria below. If the grep returns more than zero matches after the fix, stop and report before continuing.
</action>
<verify>
<automated>rg 'from "\.\./lib/' berrygems/extensions/dragon-breath/ | wc -l</automated>
</verify>
<acceptance_criteria> - `rg 'from "\.\./lib/' berrygems/extensions/dragon-breath/ | wc -l` returns `0` - `rg 'from "\.\./\.\./lib/settings\.ts"' berrygems/extensions/dragon-breath/index.ts | wc -l` returns `1` - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0 with no output (run from repo root `/home/dot/Development/pantry`)
</acceptance_criteria>
<done>
dragon-breath/index.ts line 20 uses `../../lib/settings.ts`; tsc returns zero errors.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries introduced. This is an internal import-path correction.

## STRIDE Threat Register

| Threat ID | Category | Component              | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | -------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01-01   | N/A      | dragon-breath/index.ts | accept      | No new attack surface; single-character path correction on an internal relative import. No user input crosses this boundary; no new network, filesystem, or privilege surface. |

</threat_model>

<verification>
After the task:
1. `pnpm --dir berrygems exec tsc --project tsconfig.json` must exit 0. This is THE gate for the whole milestone per STATE.md blockers.
2. `rg 'from "\.\./lib/' berrygems/extensions/dragon-breath/` must return zero matches.
3. `rg 'from "\.\./\.\./lib/settings\.ts"' berrygems/extensions/dragon-breath/index.ts` must return exactly one match.
</verification>

<success_criteria>
ROADMAP §Phase 1 success criterion 5: `tsc --project berrygems/tsconfig.json` returns zero errors. This plan closes it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-amputation-cleanup-tsc-green/01-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Commit scope: `amp` (per STATE.md anti-features §Commit scopes). Suggested message: `amp(01): fix dragon-breath import depth to unblock tsc`.
</output>
