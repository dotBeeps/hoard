---
phase: 01-amputation-cleanup-tsc-green
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - storybook-daemon
  - psi
  - allies-parity
  - dragon-cubed
  - berrygems/extensions/hoard-allies
autonomous: true
requirements: [AMP-01]
must_haves:
  truths:
    - "All five amputation-husk directories are absent from the working tree"
    - "ls of each husk path returns 'No such file or directory'"
  artifacts:
    - path: "storybook-daemon"
      provides: "must NOT exist"
    - path: "psi"
      provides: "must NOT exist"
    - path: "allies-parity"
      provides: "must NOT exist"
    - path: "dragon-cubed"
      provides: "must NOT exist"
    - path: "berrygems/extensions/hoard-allies"
      provides: "must NOT exist"
  key_links: []
---

<objective>
Remove all five amputation-husk directories from the working tree. Pure filesystem deletion — no code changes. Closes AMP-01.

Purpose: These directories were deleted from git tracking in the 2026-04-22 amputation commit but their untracked contents (build artifacts, node_modules, stray claude rules) persist on disk. They are referenced by no live code; leaving them in the tree confuses `rg`, `ls`, and new contributors and wastes ~71 MB.

Output: Five paths gone. Working tree reflects post-amputation scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete five amputation-husk directories</name>
  <files>storybook-daemon, psi, allies-parity, dragon-cubed, berrygems/extensions/hoard-allies</files>
  <read_first>
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "AMP-01: Husk Directories" (confirms all five exist on disk and are untracked)
    - .planning/REQUIREMENTS.md AMP-01 (authoritative requirement wording)
  </read_first>
  <action>
Per AMP-01 (REQUIREMENTS.md) and RESEARCH.md §AMP-01 Husk Directories table (all five verified present on live tree 2026-04-22, total ~71 MB):

| Path                                 | Contents                                                             |
| ------------------------------------ | -------------------------------------------------------------------- |
| `storybook-daemon/`                  | `internal/`, `.claude/rules/go.md`, stray `.pi/agents/*.md` — 180 KB |
| `psi/`                               | `build/` (Qt/QML build artifacts — gitignored) — 33 MB               |
| `allies-parity/`                     | `runner/node_modules/` (full npm install) — 38 MB                    |
| `dragon-cubed/`                      | `.pi/todos/dragon-cubed.md` stub only — 16 KB                        |
| `berrygems/extensions/hoard-allies/` | `.claude/rules/typescript.md` only — 16 KB                           |

Use fish (per AGENTS.md §Verification fish-not-bash rule). Run from repo root:

```fish
rm -rf storybook-daemon psi allies-parity dragon-cubed berrygems/extensions/hoard-allies
```

Do NOT use bash. Do NOT touch `berrygems/extensions/hoard-allies/` content prior to deletion — delete outright. Do NOT delete any path not listed above.

After the deletion, run `tsc --project berrygems/tsconfig.json` from repo root — it must still exit 0 (should be unaffected since none of these are imported, but verify).
</action>
<verify>
<automated>ls storybook-daemon psi allies-parity dragon-cubed berrygems/extensions/hoard-allies 2>&1 | grep -c 'No such file'</automated>
</verify>
<acceptance_criteria> - `ls storybook-daemon psi allies-parity dragon-cubed berrygems/extensions/hoard-allies 2>&1 | grep -c 'No such file'` returns `5` - `test ! -e storybook-daemon; and test ! -e psi; and test ! -e allies-parity; and test ! -e dragon-cubed; and test ! -e berrygems/extensions/hoard-allies; and echo OK` prints `OK` - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0 (no regression from Plan 01)
</acceptance_criteria>
<done>
All five husk paths absent; tsc still green.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries introduced. Filesystem deletion of abandoned directories.

## STRIDE Threat Register

| Threat ID | Category | Component        | Disposition | Mitigation Plan                                                                                                                                                                                                                       |
| --------- | -------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-02   | N/A      | husk directories | accept      | No new attack surface; removing dead directories cannot introduce threats. Deleted paths contain only build artifacts and stray doc files — no secrets, no user data, no running services. Verified by RESEARCH.md §AMP-01 inventory. |

</threat_model>

<verification>
- `ls storybook-daemon psi allies-parity dragon-cubed berrygems/extensions/hoard-allies 2>&1 | grep -c 'No such file'` returns 5.
- `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0 (regression check against Plan 01's unblocked gate).
</verification>

<success_criteria>
ROADMAP §Phase 1 success criterion 1: `ls storybook-daemon psi allies-parity dragon-cubed berrygems/extensions/hoard-allies` returns "No such file or directory" for all five paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-amputation-cleanup-tsc-green/01-02-SUMMARY.md`.

Commit scope: `amp`. Suggested message: `amp(01): remove five amputation-husk directories`.
</output>
