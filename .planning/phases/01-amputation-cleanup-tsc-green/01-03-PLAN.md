---
phase: 01-amputation-cleanup-tsc-green
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .claude/settings.json
  - .claude/hooks/stop-doc-sync.fish
  - .claude/parity-map.json
  - .claude/agents/soul-reviewer.md
  - .claude/skills/hoard-verify
  - AGENTS.override.md
autonomous: true
requirements: [AMP-02]
must_haves:
  truths:
    - "No /home/dot/Development/hoard/ path references remain in .claude/ or AGENTS.override.md"
    - ".claude/parity-map.json is deleted (D-06)"
    - ".claude/hooks/stop-doc-sync.fish is deleted (D-07)"
    - ".claude/agents/soul-reviewer.md is deleted (D-06 cascade)"
    - ".claude/skills/hoard-verify/ directory is deleted (D-06 cascade)"
    - ".claude/settings.json retains only the PreToolUse pre-block-gosum.fish hook with a corrected pantry path"
    - "AGENTS.override.md contains the pantry-shaped template (D-09) with no storybook-ember/stone/dragon-forge/dragon-cubed references"
  artifacts:
    - path: ".claude/settings.json"
      provides: "Hook registrations pointing at live pantry paths only"
      contains: "/home/dot/Development/pantry/.claude/hooks/pre-block-gosum.fish"
    - path: "AGENTS.override.md"
      provides: "Pantry-shaped local override template"
      contains: "Local Pi Path"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/pre-block-gosum.fish"
      via: "fish command invocation in PreToolUse hook"
      pattern: "fish /home/dot/Development/pantry/\\.claude/hooks/pre-block-gosum\\.fish"
---

<objective>
Sweep all `/home/dot/Development/hoard/` path references and amputation-era config from `.claude/` and `AGENTS.override.md`. Implements D-06, D-07, D-08, D-09. Closes AMP-02.

Purpose: Every Stop hook in `.claude/settings.json` references an amputated subsystem (`dragon-forge`, `cc-plugin`, or `stop-doc-sync.fish`'s hoard-scoped parity map). Path-correcting them would preserve dead logic that runs every session. Per research: the ONLY surviving hook is `pre-block-gosum.fish` (harmless no-op in a Go-file-free repo). Plus: `soul-reviewer.md` and `hoard-verify/` reference amputated MCPs and amputated disk paths — full removal. Plus: `AGENTS.override.md` template is a local-only gitignored file; user opted for full rewrite over minimal scrub (D-09).

Output: Post-amputation `.claude/` config; rewritten `AGENTS.override.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md
@AGENTS.md
@.claude/settings.json
@AGENTS.override.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete amputated .claude/ artifacts (D-06, D-07 cascade)</name>
  <files>.claude/parity-map.json, .claude/hooks/stop-doc-sync.fish, .claude/agents/soul-reviewer.md, .claude/skills/hoard-verify</files>
  <read_first>
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decisions D-06, D-07 and "### .claude/ sweep scope"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "AMP-02: Stale /home/dot/Development/hoard/ Path References" (soul-reviewer and hoard-verify disposition)
    - .claude/parity-map.json (top 30 lines; confirm it is cc-plugin parity content)
    - .claude/hooks/stop-doc-sync.fish (confirm hard-codes hoard_prefixes / cc-plugin paths)
    - .claude/agents/soul-reviewer.md (confirm references hoard daemon + storybook-ember MCP)
    - .claude/skills/hoard-verify/SKILL.md (confirm references storybook-daemon + psi QML paths)
  </read_first>
  <action>
Per D-06 (CONTEXT.md) + D-07 (CONTEXT.md) + RESEARCH.md §AMP-02:

Delete the following four paths outright (fish, run from repo root):

```fish
rm -f .claude/parity-map.json
rm -f .claude/hooks/stop-doc-sync.fish
rm -f .claude/agents/soul-reviewer.md
rm -rf .claude/skills/hoard-verify
```

Rationale per entry:

- `.claude/parity-map.json` (D-06): 147 lines of cc-plugin parity; cc-plugin is amputated; every entry points at deleted disk artifacts.
- `.claude/hooks/stop-doc-sync.fish` (D-07): Logic exists to keep parity-map.json in sync with live code; with the map deleted, the hook has nothing to check; hard-codes `hoard_prefixes` and `cc-plugin/agents/` paths.
- `.claude/agents/soul-reviewer.md` (RESEARCH.md §AMP-02): References `hoard daemon`, reads `/home/dot/Development/hoard/ETHICS.md`, uses `mcp__storybook-ember__*` tools (amputated MCP).
- `.claude/skills/hoard-verify/` (RESEARCH.md §AMP-02): Invokes `cd /home/dot/Development/hoard/storybook-daemon` and `qmllint /home/dot/Development/hoard/psi/qml/…` — both targets amputated.

Do NOT delete `.claude/hooks/pre-block-gosum.fish`, `.claude/hooks/stop-phase-gate.fish`, or `.claude/hooks/stop-parity-check.fish`. Those files remain on disk (Task 2 handles their registration in settings.json).
</action>
<verify>
<automated>test ! -e .claude/parity-map.json; and test ! -e .claude/hooks/stop-doc-sync.fish; and test ! -e .claude/agents/soul-reviewer.md; and test ! -e .claude/skills/hoard-verify; and echo OK</automated>
</verify>
<acceptance_criteria> - `test ! -e .claude/parity-map.json; and echo OK` prints `OK` - `test ! -e .claude/hooks/stop-doc-sync.fish; and echo OK` prints `OK` - `test ! -e .claude/agents/soul-reviewer.md; and echo OK` prints `OK` - `test ! -e .claude/skills/hoard-verify; and echo OK` prints `OK` - `test -e .claude/hooks/pre-block-gosum.fish; and test -e .claude/hooks/stop-phase-gate.fish; and test -e .claude/hooks/stop-parity-check.fish; and echo OK` prints `OK` (these files must NOT be deleted)
</acceptance_criteria>
<done>
Four amputated `.claude/` artifacts removed; three other hook files still present on disk.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite .claude/settings.json to pantry-only PreToolUse hook (D-08)</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json (current full file, 33 lines)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decision D-08
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md sections "AMP-02: Stale /home/dot/Development/hoard/ Path References" and ".claude/ Hook Disposition Table" (assumptions A1, A2 documented)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md section ".claude/settings.json — D-07 + D-08 rewrite" (exact target JSON)
  </read_first>
  <action>
Per D-08 (CONTEXT.md) + RESEARCH.md §.claude/ Hook Disposition Table + PATTERNS.md §.claude/settings.json rewrite:

Disposition per hook (verified by RESEARCH.md §AMP-02):

| Hook                                | Action                                               | Reason                                                                                                                |
| ----------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `pre-block-gosum.fish` (PreToolUse) | KEEP, path-fix to `/home/dot/Development/pantry/...` | No Go files in pantry so it never fires; harmless; removing a PreToolUse safety hook is riskier than keeping it (A2). |
| `stop-phase-gate.fish` (Stop)       | DROP registration                                    | References amputated `dragon-forge/` phase artifacts; warning vacuously empty every session (A1).                     |
| `stop-parity-check.fish` (Stop)     | DROP registration                                    | Reads `cc-plugin/` and `parity-map.json` (Task 1 deleted the map); cannot produce valid output (A1).                  |
| `stop-doc-sync.fish` (Stop)         | DROP registration                                    | File deleted in Task 1 (D-07).                                                                                        |

Result: `Stop` hook array becomes empty. Drop the entire `Stop` key from the JSON rather than leaving `"Stop": [{"hooks": []}]`. Only the `PreToolUse` key survives.

Overwrite `.claude/settings.json` with EXACTLY this content (copy verbatim from PATTERNS.md §.claude/settings.json rewrite):

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "fish /home/dot/Development/pantry/.claude/hooks/pre-block-gosum.fish"
          }
        ]
      }
    ]
  }
}
```

Preserve trailing newline. No other keys, no comments (JSON does not support them).
</action>
<verify>
<automated>rg '/home/dot/Development/hoard/' .claude/settings.json | wc -l</automated>
</verify>
<acceptance_criteria> - `rg '/home/dot/Development/hoard/' .claude/settings.json | wc -l` returns `0` - `rg '/home/dot/Development/pantry/\.claude/hooks/pre-block-gosum\.fish' .claude/settings.json | wc -l` returns `1` - `rg 'stop-phase-gate|stop-parity-check|stop-doc-sync' .claude/settings.json | wc -l` returns `0` - `jq -e '.hooks.PreToolUse | length == 1' .claude/settings.json` exits 0 (valid JSON, exactly one PreToolUse entry) - `jq -e 'has("hooks") and (.hooks | has("Stop") | not)' .claude/settings.json` exits 0 (no Stop key)
</acceptance_criteria>
<done>
settings.json contains only the PreToolUse pre-block-gosum.fish hook, pointing at the pantry path. No Stop hooks registered. Valid JSON.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full rewrite of AGENTS.override.md template (D-09)</name>
  <files>AGENTS.override.md</files>
  <read_first>
    - AGENTS.override.md (current 22-line content, confirm it still references storybook-ember MCP, stone HTTP bus, dragon-forge, dragon-cubed)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decision D-09 and specific idea "D-09 AGENTS.override.md template"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "AGENTS.override.md Template Shape (D-09)"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md section "AGENTS.override.md — D-09 full rewrite"
  </read_first>
  <action>
Per D-09 (CONTEXT.md — user acknowledged risk of losing personal customization; explicit opt-in to full rewrite over minimal scrub) + RESEARCH.md §AGENTS.override.md Template Shape + PATTERNS.md §AGENTS.override.md full rewrite:

Overwrite `AGENTS.override.md` with EXACTLY this content (verbatim from PATTERNS.md target state):

```markdown
# AGENTS.override.md

Local development overrides — gitignored, never committed.

## Local Pi Path

If running pi from local source rather than the installed npm package, note the
path here so commands in skills can be adjusted.

## Machine-Specific Notes

Add anything about this dev environment that affects agent behavior:

- Custom `pantry.*` settings active locally (dev overrides, experimental flags)
- Non-standard pi installation paths
- Any external tools the berrygems depend on (e.g., if testing GIF rendering)
```

Preserve trailing newline. Dropped sections: Local Ports, Active Experiments, GPU/Minecraft/MCP references. Do NOT add any mention of storybook-ember, storybook-maren, stone HTTP bus, dragon-forge, or dragon-cubed.
</action>
<verify>
<automated>rg 'storybook-ember|storybook-maren|stone HTTP|dragon-forge|dragon-cubed|/home/dot/Development/hoard/' AGENTS.override.md | wc -l</automated>
</verify>
<acceptance_criteria> - `rg 'storybook-ember|storybook-maren|stone HTTP|dragon-forge|dragon-cubed|/home/dot/Development/hoard/' AGENTS.override.md | wc -l` returns `0` - `rg '^## Local Pi Path$' AGENTS.override.md | wc -l` returns `1` - `rg '^## Machine-Specific Notes$' AGENTS.override.md | wc -l` returns `1` - `rg 'pantry\.\*' AGENTS.override.md | wc -l` returns `1`
</acceptance_criteria>
<done>
AGENTS.override.md replaced with pantry-shaped template; zero amputated-subsystem references.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final AMP-02 grep-gate verification</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §Phase 1 success criterion 2
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "Verification Gate Commands"
  </read_first>
  <action>
Run the ROADMAP success criterion 2 verification suite from repo root. All three commands must pass. If any fails, fix the residue before marking the task done — do not mark complete and defer.

```fish
rg '/home/dot/Development/hoard/' .claude AGENTS.override.md
# must return: zero matches (exit 1 from rg)

test ! -e .claude/agents/soul-reviewer.md; and echo "OK soul-reviewer gone"
test ! -e .claude/skills/hoard-verify; and echo "OK hoard-verify gone"
```

Also run tsc regression gate:

```fish
pnpm --dir berrygems exec tsc --project tsconfig.json
# must exit 0
```

  </action>
  <verify>
    <automated>rg '/home/dot/Development/hoard/' .claude AGENTS.override.md; or echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `rg '/home/dot/Development/hoard/' .claude AGENTS.override.md` exits 1 (no matches, rg convention)
    - `test ! -e .claude/agents/soul-reviewer.md` exits 0
    - `test ! -e .claude/skills/hoard-verify` exits 0
    - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    ROADMAP §Phase 1 success criterion 2 is green; tsc still green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries introduced. All changes are local-only config edits and deletions of dead artifacts.

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-03   | Tampering              | .claude/settings.json | accept      | Hook command points at a local filesystem fish path; path is hardcoded to pantry tree. No user-controlled input flows into hook command assembly.                |
| T-01-04   | Denial of Service      | Dropped Stop hooks    | accept      | Removing Stop hooks cannot DoS anything — their logic was already vacuous against amputated subsystems (A1). The PreToolUse gosum hook survives as a safety net. |
| T-01-05   | Information Disclosure | AGENTS.override.md    | accept      | File is gitignored (user-confirmed D-09) and contains only template structure with no secrets. Rewrite removes historical references to amputated local ports.   |

</threat_model>

<verification>
- `rg '/home/dot/Development/hoard/' .claude AGENTS.override.md` returns zero matches.
- `.claude/agents/soul-reviewer.md` and `.claude/skills/hoard-verify/` absent.
- `.claude/parity-map.json` and `.claude/hooks/stop-doc-sync.fish` absent.
- `.claude/settings.json` is valid JSON with only one PreToolUse hook, no Stop key.
- `AGENTS.override.md` contains only the pantry-shaped template.
- `pnpm --dir berrygems exec tsc --project tsconfig.json` still exits 0.
</verification>

<success_criteria>
ROADMAP §Phase 1 success criterion 2: `rg '/home/dot/Development/hoard/' .claude AGENTS.override.md` returns zero matches; `.claude/agents/soul-reviewer.md` and `.claude/skills/hoard-verify/` are deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/01-amputation-cleanup-tsc-green/01-03-SUMMARY.md`.

Commit scope: `amp`. Suggested message: `amp(01): sweep .claude/ hoard paths and rewrite AGENTS.override template`.
</output>
