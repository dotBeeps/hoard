---
phase: 01-amputation-cleanup-tsc-green
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - morsels/skills/hoard-allies
  - morsels/skills/hoard-sending-stone
  - berrygems/extensions/dragon-guard/index.ts
  - berrygems/extensions/dragon-guard/state.ts
  - berrygems/extensions/dragon-guard/AGENTS.md
  - berrygems/lib/panel-chrome.ts
  - berrygems/lib/pi-spawn.ts
  - berrygems/extensions/dragon-digestion.ts
  - berrygems/AGENTS.md
  - morsels/AGENTS.md
autonomous: true
requirements: [AMP-03]
must_haves:
  truths:
    - 'No Symbol.for("hoard.*") strings remain anywhere in morsels/ or berrygems/ (persona flavor prose in dragon-curfew / dragon-musings untouched)'
    - "morsels/skills/hoard-allies/ and morsels/skills/hoard-sending-stone/ directories deleted outright"
    - "dragon-guard/index.ts ally-mode block (lines 188-219) and its accompanying comment (line 220) deleted"
    - 'dragon-guard/state.ts: initAllyMode, isAllyMode, getAllyModeToolPolicy, _allyToolWhitelist, GuardMode "ally" arm, MODE_LABEL "ally" entry all removed'
    - "dragon-guard/index.ts import from ./state.ts no longer imports initAllyMode or getAllyModeToolPolicy"
    - "dragon-guard/AGENTS.md line 13 Ally Mode bullet deleted"
    - "panel-chrome.ts lines 127, 289 no longer say 'hoard vibes' / 'hoard aesthetic'"
    - "pi-spawn.ts line 9 hoard-allies attribution removed"
    - "dragon-digestion.ts 1938-1945 JSDoc block + commented-out ANTHROPIC_CONTEXT_MGMT_BETA const deleted"
    - "berrygems/AGENTS.md line 16 daemon-present-tense framing rewritten; line 70 cd path corrected"
    - "morsels/AGENTS.md line 13 daemon-present-tense framing rewritten"
    - "tsc --project berrygems/tsconfig.json still exits 0 after all edits"
  artifacts:
    - path: "berrygems/extensions/dragon-guard/state.ts"
      provides: "GuardMode type without ally arm; no ally-mode helpers"
      contains: "export type GuardMode"
    - path: "berrygems/extensions/dragon-guard/index.ts"
      provides: "dragon-guard entrypoint without ally-mode branch"
  key_links:
    - from: "berrygems/extensions/dragon-guard/index.ts"
      to: "berrygems/extensions/dragon-guard/state.ts"
      via: "import destructure must not include initAllyMode / getAllyModeToolPolicy"
      pattern: "from \"\\./state\\.ts\""
---

<objective>
Sweep all `hoard.*` API-string residue, stale env-var references, dead ally-mode code, and daemon-present-tense documentation from shipped code and morsels. Implements D-04, D-05, D-10, D-11, D-12. Closes AMP-03.

Purpose: The `hoard-allies` extension that fed dragon-guard's ally-mode branch is amputated. The branch is dead code on every pi startup. Two `hoard-*` morsel skills document APIs that no longer exist. Several JSDoc comments carry `hoard vibes` / `hoard-allies` attribution. Two AGENTS.md files present `storybook-daemon` as the "persistent core" — factually wrong post-amputation. All are sweep-and-fix with precise file:line inventory from RESEARCH.md.

Output: Dead code and stale docs removed; `tsc` still green; persona flavor in `dragon-curfew`/`dragon-musings` preserved per the AMP-03 carve-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md
@.planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md
@AGENTS.md
@berrygems/AGENTS.md

<interfaces>
Dragon-guard state.ts current shape (RESEARCH.md §AMP-03 D-04, verified live tree):

```typescript
// state.ts:13
export type GuardMode = "none" | "plan" | "dragon" | "ally";

// state.ts:22-27
export const MODE_LABEL: Record<GuardMode, string> = {
  none: "Dog Mode",
  plan: "Puppy Mode",
  dragon: "Dragon Mode",
  ally: "Ally Mode",   // delete
};

// state.ts:34 — delete
let _allyToolWhitelist: Set<string> | null = null;

// state.ts:55-73 — delete all three functions
export function initAllyMode(tools: string[]): void { ... }
export function isAllyMode(): boolean { ... }
export function getAllyModeToolPolicy(toolName: string): "allow" | "block" { ... }
```

After edits: `GuardMode` becomes `"none" | "plan" | "dragon"`; MODE_LABEL loses the ally entry; the three functions and the whitelist variable disappear.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete hoard-* morsel skills (D-05)</name>
  <files>morsels/skills/hoard-allies, morsels/skills/hoard-sending-stone</files>
  <read_first>
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decision D-05 and specific idea "D-05 deletion cascade"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md sections "AMP-03 > Symbol.for(\"hoard.*\") — morsels" and "HOARD_* env var references — morsels" (verified residue scoped to these two directories)
    - morsels/skills/hoard-allies/SKILL.md (confirm still exists and references `hoard.allies`)
    - morsels/skills/hoard-sending-stone/SKILL.md (confirm still exists and references `hoard.stone` + HOARD_* env vars)
  </read_first>
  <action>
Per D-05 (CONTEXT.md — explicit `rm -rf`, no tombstone stubs) + RESEARCH.md §AMP-03:

All three surviving `Symbol.for("hoard.*")` matches and both `HOARD_*` env-var matches in the morsels tree are inside these two directories. Pi's `morsels/skills/*/SKILL.md` glob auto-drops deleted dirs from discovery.

Run from repo root (fish):

```fish
rm -rf morsels/skills/hoard-allies
rm -rf morsels/skills/hoard-sending-stone
```

Do NOT delete any other `morsels/skills/` entry. Do NOT leave placeholder SKILL.md files. Do NOT touch `den/features/hoard-allies/` — per D-13, `den/` is internal archive and out of scope (the one live inbound cross-reference into `den/` disappears with this deletion).
</action>
<verify>
<automated>test ! -e morsels/skills/hoard-allies; and test ! -e morsels/skills/hoard-sending-stone; and echo OK</automated>
</verify>
<acceptance_criteria> - `test ! -e morsels/skills/hoard-allies; and echo OK` prints `OK` - `test ! -e morsels/skills/hoard-sending-stone; and echo OK` prints `OK` - `rg 'Symbol\.for\("hoard\.' morsels | wc -l` returns `0` - `rg 'HOARD_STONE_PORT|HOARD_ALLY_DEFNAME' morsels | wc -l` returns `0`
</acceptance_criteria>
<done>
Both `hoard-*` skill directories gone; no `Symbol.for("hoard.*")` or `HOARD*\*` env-var residue in morsels.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete dragon-guard ally-mode block and state.ts helpers (D-04)</name>
  <files>berrygems/extensions/dragon-guard/index.ts, berrygems/extensions/dragon-guard/state.ts, berrygems/extensions/dragon-guard/AGENTS.md</files>
  <read_first>
    - berrygems/extensions/dragon-guard/index.ts (lines 30-230, confirm line 37 imports from ./state.ts, lines 188-219 contain the `if (guardModeEnv === "ally" && allyToolsEnv)` block, line 220 has the "Legacy subagent bail-out (non-hoard subagents)" comment)
    - berrygems/extensions/dragon-guard/state.ts (full file, confirm lines 13, 22-27, 34, 55-73 contain the ally-mode surface)
    - berrygems/extensions/dragon-guard/AGENTS.md (first 30 lines, confirm line 13 is the Ally Mode bullet and it is the only `hoard` reference in the file)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decision D-04
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "Dragon-guard ally-mode block (D-04)" (corrects CONCERNS.md env-var names — live code uses PANTRY_* not HOARD_*)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md sections "dragon-guard/index.ts — D-04 ally-mode block deletion", "dragon-guard/state.ts — D-04 function deletions", "dragon-guard/AGENTS.md — D-04 prose scrub"
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md §Common Pitfalls Pitfall 1 (delete state.ts exports or tsc flags unused imports)
  </read_first>
  <action>
Per D-04 (CONTEXT.md) + RESEARCH.md §Dragon-guard ally-mode block (D-04) + PATTERNS.md. RESEARCH.md correction applies: env-var names in live code are `PANTRY_GUARD_MODE` / `PANTRY_ALLY_TOOLS` (NOT `HOARD_*` as CONCERNS.md said). The deletion target is still the same block.

**Sub-step A — `dragon-guard/index.ts`:**

Delete lines 188-219 inclusive (the comment `// ── Ally Mode: quest-dispatched allies get locked tool whitelist ──` and the entire block through the closing `return;`), plus the comment at line 220 (`// ── Legacy subagent bail-out (non-hoard subagents) ──`).

The function body at approximately line 220 should resume directly with the `const subagentDepth = Number(process.env.PI_SUBAGENT_DEPTH ?? "0");` line. Per PATTERNS.md: rename the removed comment to a neutral replacement above this line, for example `// ── Subagent guard — extensions only run in primary sessions ──`.

Update the import at line 37 (the destructure from `./state.ts`): remove `initAllyMode` and `getAllyModeToolPolicy` from the destructured list. Leave other imports from `state.ts` intact.

**Sub-step B — `dragon-guard/state.ts`:**

- Line 13: change `export type GuardMode = "none" | "plan" | "dragon" | "ally";` → `export type GuardMode = "none" | "plan" | "dragon";`
- Lines 22-27 `MODE_LABEL`: remove the `ally: "Ally Mode",` entry
- Line 34 (approx): delete `let _allyToolWhitelist: Set<string> | null = null;`
- Lines 55-73 (approx): delete all three functions — `export function initAllyMode(tools: string[]): void { ... }`, `export function isAllyMode(): boolean { ... }`, `export function getAllyModeToolPolicy(toolName: string): "allow" | "block" { ... }`

After edits, run `tsc --project berrygems/tsconfig.json` from repo root — if tsc flags any remaining reference, trace and fix. Per Pitfall 1, tsc's `noUnusedLocals` is the safety net here.

**Sub-step C — `dragon-guard/AGENTS.md`:**

Delete the entire line 13 bullet (per PATTERNS.md §dragon-guard/AGENTS.md — D-04 prose scrub):

```
- **Ally Mode** (`ally`): Quest-dispatched allies only. Tool whitelist set by hoard-allies at spawn time via env vars. No prompting — whitelisted tools execute, everything else is silently blocked. Cannot escalate to any other mode. Set at process birth, immutable.
```

The "Four modes" list becomes three modes. Per RESEARCH.md §AMP-03: line 13 is the ONLY hoard reference in this file (lines 104 and 161 referenced by CONCERNS.md do not exist in the live 172-line file). Do not edit beyond line 13.

**Ordering note:** Do sub-step A before B if you want tsc to guide you to each state.ts site. Doing B before A produces tsc errors at the index.ts call sites, which is also fine as a guide. Pick whichever ordering produces fewer intermediate errors; the end state is the same.
</action>
<verify>
<automated>pnpm --dir berrygems exec tsc --project tsconfig.json</automated>
</verify>
<acceptance_criteria> - `rg 'PANTRY_GUARD_MODE|PANTRY_ALLY_TOOLS' berrygems/extensions/dragon-guard/ | wc -l` returns `0` - `rg 'initAllyMode|getAllyModeToolPolicy|isAllyMode|_allyToolWhitelist' berrygems/extensions/dragon-guard/ | wc -l` returns `0` - `rg '"ally"' berrygems/extensions/dragon-guard/state.ts | wc -l` returns `0` - `rg 'Legacy subagent bail-out \(non-hoard' berrygems/extensions/dragon-guard/index.ts | wc -l` returns `0` - `rg '^- \*\*Ally Mode\*\*' berrygems/extensions/dragon-guard/AGENTS.md | wc -l` returns `0` - `rg 'hoard' berrygems/extensions/dragon-guard/AGENTS.md | wc -l` returns `0` - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0
</acceptance_criteria>
<done>
Ally-mode code surface removed from dragon-guard index.ts + state.ts + AGENTS.md; tsc clean.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scrub hoard-flavor comments and factual errors in lib + AGENTS (D-10, D-11, D-12)</name>
  <files>berrygems/lib/panel-chrome.ts, berrygems/lib/pi-spawn.ts, berrygems/extensions/dragon-digestion.ts, berrygems/AGENTS.md, morsels/AGENTS.md</files>
  <read_first>
    - berrygems/lib/panel-chrome.ts (read lines 120-135 and 285-295, confirm lines 127 and 289 JSDoc content)
    - berrygems/lib/pi-spawn.ts (read lines 1-20, confirm line 9 attribution text)
    - berrygems/extensions/dragon-digestion.ts (read lines 1930-1960, confirm lines 1938-1945 block shape and commented-out const)
    - berrygems/AGENTS.md (read lines 10-75, confirm line 16 framing and line 70 cd path)
    - morsels/AGENTS.md (read lines 1-25, confirm line 13 framing)
    - .planning/phases/01-amputation-cleanup-tsc-green/01-CONTEXT.md decisions D-10, D-11, D-12
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "Hoard-flavor comments in berrygems code (D-10, D-11, D-12)" table
    - .planning/phases/01-amputation-cleanup-tsc-green/01-PATTERNS.md sections "berrygems/lib/panel-chrome.ts — D-10 JSDoc scrub", "berrygems/lib/pi-spawn.ts — D-11 attribution scrub", "berrygems/extensions/dragon-digestion.ts — D-11 stale TODO scrub", "berrygems/AGENTS.md — D-12 factual fixes", "morsels/AGENTS.md — D-12 factual fix"
  </read_first>
  <action>
Per D-10, D-11, D-12 (CONTEXT.md) + RESEARCH.md §Hoard-flavor table + PATTERNS.md exact before/after.

**Sub-step A — `berrygems/lib/panel-chrome.ts` (D-10):**

Line 127 BEFORE:

```typescript
/** Dots and sparkles. Whimsical hoard vibes. */
```

Line 127 AFTER:

```typescript
/** Dots and sparkles. Whimsical vibes. */
```

Line 289 BEFORE:

```typescript
/** Ice crystal edges. Frozen hoard aesthetic. ❈ */
```

Line 289 AFTER:

```typescript
/** Ice crystal edges. Frozen aesthetic. ❈ */
```

**Sub-step B — `berrygems/lib/pi-spawn.ts` (D-11):**

Line 9 BEFORE (inside file-level JSDoc):

```
 * Extracted from berrygems/extensions/hoard-allies/spawn.ts for use across
 * any extension that needs to dispatch pi subprocesses.
```

Per PATTERNS.md §pi-spawn.ts: delete this line entirely (both `* Extracted...` and `* any extension...` — the two-line sentence). The module-description JSDoc above (lines 3-8) already fully describes what the file is. Retain the rest of the JSDoc block. Discretion per CONTEXT.md allows either removal or rewrite; the research confirms the sentence documents a migration from an amputated extension and provides no current orientation value — delete is correct.

**Sub-step C — `berrygems/extensions/dragon-digestion.ts` (D-11):**

Lines 1938-1945 BEFORE (RESEARCH.md §AMP-03 + PATTERNS.md §dragon-digestion.ts):

```typescript
/**
 * Anthropic beta feature needed for context_management API.
 * NOT currently injected — registerProvider header override strips OAuth betas
 * (claude-code-20250219, oauth-2025-04-20), causing 401 for OAuth users.
 * Blocked until hoard-lab extension can detect auth type and merge headers safely.
 * See: den/plans/dragon-digestion-v2.md "Beta Header Setup" section.
 */
// const ANTHROPIC_CONTEXT_MGMT_BETA = "context-management-2025-06-27";
```

AFTER: delete the entire block (JSDoc comment AND the commented-out const on the next line). Per RESEARCH.md Assumptions Log A3: the beta API remains blocked for a separate reason (OAuth header stripping); keeping a stale hoard-lab reference alongside a commented-out const provides no signal. The surrounding code continues unchanged from line 1937 to what was line 1946.

**Sub-step D — `berrygems/AGENTS.md` (D-12):**

Line 16 BEFORE:

```
- **storybook-daemon** is the persistent core — mind, soul, connectors. berrygems tools are what the daemon _uses_ when inhabiting a pi session.
```

Line 16 AFTER (per PATTERNS.md §berrygems/AGENTS.md — exact prose is discretion but must remove daemon-present-tense):

```
- **berrygems** are pi extensions — self-contained tools that run within a pi session. Each extension loads independently; no persistent daemon coordinates them.
```

Line 70 BEFORE:

```
cd /home/dot/Development/hoard && tsc --project berrygems/tsconfig.json
```

Line 70 AFTER (per PATTERNS.md — drop the wrong cd entirely; AGENTS.md:88 already documents the correct cwd):

```
tsc --project berrygems/tsconfig.json
```

**Sub-step E — `morsels/AGENTS.md` (D-12):**

Line 13 BEFORE:

```
- **storybook-daemon** is the persistent core. Morsels are portable knowledge any body can consume — during a pi session, through a daemon-directed subagent, or standalone.
```

Line 13 AFTER (per PATTERNS.md §morsels/AGENTS.md — exact prose discretionary, framing must change):

```
- **berrygems** is the programmatic tool layer. Some morsels teach agents how to use berrygems APIs directly.
```

**Do NOT touch** `dragon-curfew.ts`, `dragon-musings.ts`, or `ETHICS.md` — these are explicitly carved out of AMP-03 scope per CONTEXT.md §domain and §deferred. The AMP-03 grep gate is scoped to `Symbol.for("hoard.*")` strings; persona "hoard" flavor prose is allowed to remain.
</action>
<verify>
<automated>pnpm --dir berrygems exec tsc --project tsconfig.json</automated>
</verify>
<acceptance_criteria> - `rg 'Whimsical hoard vibes|Frozen hoard aesthetic' berrygems/lib/panel-chrome.ts | wc -l` returns `0` - `rg 'Extracted from berrygems/extensions/hoard-allies' berrygems/lib/pi-spawn.ts | wc -l` returns `0` - `rg 'hoard-lab extension can detect auth type' berrygems/extensions/dragon-digestion.ts | wc -l` returns `0` - `rg 'ANTHROPIC_CONTEXT_MGMT_BETA' berrygems/extensions/dragon-digestion.ts | wc -l` returns `0` - `rg 'storybook-daemon is the persistent core' berrygems/AGENTS.md morsels/AGENTS.md | wc -l` returns `0` - `rg 'cd /home/dot/Development/hoard' berrygems/AGENTS.md | wc -l` returns `0` - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0
</acceptance_criteria>
<done>
panel-chrome JSDoc neutralized; pi-spawn attribution removed; dragon-digestion stale-TODO block deleted; both AGENTS.md files corrected; tsc still green.
</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final AMP-03 grep-gate verification</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §Phase 1 success criterion 3
    - .planning/phases/01-amputation-cleanup-tsc-green/01-RESEARCH.md section "Verification Gate Commands" (SC-3)
  </read_first>
  <action>
Run the ROADMAP success criterion 3 verification. Per CONTEXT.md carve-out, persona "hoard" flavor prose inside `dragon-curfew` / `dragon-musings` MAY remain — the grep gate is specifically scoped to `Symbol.for("hoard.*")` API strings, which WILL be zero after this plan.

```fish
rg 'Symbol\.for\("hoard\.' morsels berrygems
# must return: zero matches (exit 1)

pnpm --dir berrygems exec tsc --project tsconfig.json
# must exit 0
```

If `rg` returns any match, trace to file and fix before marking the task done.
</action>
<verify>
<automated>rg 'Symbol\.for\("hoard\.' morsels berrygems; or echo PASS</automated>
</verify>
<acceptance_criteria> - `rg 'Symbol\.for\("hoard\.' morsels berrygems` exits 1 (no matches) - `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0
</acceptance_criteria>
<done>
ROADMAP §Phase 1 success criterion 3 is green; tsc still green.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries introduced. Scope is deletion of dead code paths and stale documentation strings.

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                  |
| --------- | ---------------------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01-06   | Elevation of Privilege | dragon-guard ally-mode removal | mitigate    | Removing the ally-mode branch REDUCES privilege surface — it was an unreachable code path that, if reached by a misconfigured env var, granted a tool whitelist. Deleting it eliminates an orphaned code path. Mitigation is the removal itself. |
| T-01-07   | N/A                    | morsel skill deletion          | accept      | Markdown skill bodies; no code executes. Removal cannot introduce threats.                                                                                                                                                                       |
| T-01-08   | N/A                    | comment/prose scrubs           | accept      | Comment and documentation changes; no runtime behavior affected.                                                                                                                                                                                 |

</threat_model>

<verification>
- `rg 'Symbol\.for\("hoard\.' morsels berrygems` returns zero matches.
- `rg 'initAllyMode|getAllyModeToolPolicy|isAllyMode|_allyToolWhitelist|PANTRY_GUARD_MODE|PANTRY_ALLY_TOOLS' berrygems/extensions/dragon-guard/` returns zero matches.
- `rg 'storybook-daemon is the persistent core' berrygems/AGENTS.md morsels/AGENTS.md` returns zero matches.
- `rg 'cd /home/dot/Development/hoard' berrygems/AGENTS.md` returns zero matches.
- `rg 'Whimsical hoard vibes|Frozen hoard aesthetic|hoard-lab extension can detect auth type|Extracted from berrygems/extensions/hoard-allies' berrygems/` returns zero matches.
- `pnpm --dir berrygems exec tsc --project tsconfig.json` exits 0.
</verification>

<success_criteria>
ROADMAP §Phase 1 success criterion 3: `rg 'Symbol\.for\("hoard\.' morsels berrygems` returns zero matches (persona flavor prose exempted).
</success_criteria>

<output>
After completion, create `.planning/phases/01-amputation-cleanup-tsc-green/01-04-SUMMARY.md`.

Commit scope: `amp`. Suggested message: `amp(01): scrub hoard API residue and daemon-present-tense prose`.
</output>
