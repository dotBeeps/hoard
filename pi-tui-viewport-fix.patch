--- a/packages/pi-tui/src/tui.ts
+++ b/packages/pi-tui/src/tui.ts
@@ -XXX,XX +XXX,XX @@ export class TUI extends Container {
 		// Render all components to get new lines
 		let newLines = this.render(width);
 
+		// When a full-clear render is imminent (width/height changed), reset maxLinesRendered
+		// BEFORE compositing overlays. compositeOverlays pads to maxLinesRendered to keep
+		// overlay positioning stable during normal (differential) renders. But on a clear
+		// render the scrollback is wiped, so the old watermark is stale. If we don't reset,
+		// the padding inflates newLines.length, fullRender(true) stores the inflated value
+		// back into maxLinesRendered, and the watermark never shrinks — pushing the viewport
+		// down by (oldMaxLines - actualContent) empty rows on every resize.
+		if (widthChanged || (heightChanged && !isTermuxSession())) {
+			this.maxLinesRendered = newLines.length;
+		}
+
 		// Composite overlays into the rendered lines (before differential compare)
 		if (this.overlayStack.length > 0) {
 			newLines = this.compositeOverlays(newLines, width, height);
